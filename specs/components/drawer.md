# Drawer Contract

## Path
- `src/components/organisms/Drawer`

## API
- Placement: `left`, `right`, `top`, `bottom` (set on `DrawerContent`)
- Sub-components: `DrawerTrigger`, `DrawerContent`, `DrawerHeader`, `DrawerTitle`, `DrawerBody`, `DrawerFooter`, `DrawerClose` (composable API)

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
