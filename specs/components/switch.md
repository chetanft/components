# Switch Contract

## Path
- `src/components/atoms/Switch`

## API
- Sizes: `sm`, `md`
- States: `checked`, `unchecked`
- Sub-components: `SwitchInput`, `SwitchLabel`, `SwitchHelper` (composable API)

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
