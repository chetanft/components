# Tabs Contract

## Path
- `src/components/organisms/Tabs`

## API
- Types: `primary`, `secondary`, `tertiary`
- Overflow behavior: `auto`, `dropdown`
- Sub-components: `TabsList`, `TabsTrigger`, `TabsContent` (composable API)

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
