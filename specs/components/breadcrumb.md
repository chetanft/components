# Breadcrumb Contract

## Path
- `src/components/molecules/Breadcrumb`

## API
- Sub-components: `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbSeparator` (composable API)
- `BreadcrumbLink` props: `href`, `icon`, `isCurrentPage`

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
