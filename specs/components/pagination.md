# Pagination Contract

## Path
- `src/components/molecules/Pagination`

## API
- Variants: `default`, `compact`
- Sub-components: `PaginationList`, `PaginationItem`, `PaginationPrevious`, `PaginationNext`, `PaginationEllipsis` (composable API)

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
