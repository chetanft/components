# Label Contract

## Path
- `src/components/atoms/Label`

## API
- Element: `label`, `span`, `div` (via `as` prop)
- Modifiers: `mandatory` (asterisk), `optional` (suffix text), `suffixIcon` (info icon)

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
