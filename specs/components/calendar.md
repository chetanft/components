# Calendar Contract

## Path
- `src/components/molecules/Calendar`

## API
- Modes: `month`, `year`, `decade`
- Locales: `en`, `zh`
- Props: `value`, `defaultValue`, `fullscreen` (boolean), `disabledDate`, `validRange`, `dateCellRender`, `monthCellRender`, `headerRender`, `weekdayLabels`, `monthLabels`, `monthLabelsFull`, `glass` (GlassVariant)
- Callbacks: `onSelect`, `onPanelChange`

## Token Rules
- No hex literals in component styles.
- Spacing and dimensions must resolve through tokens/classes.
