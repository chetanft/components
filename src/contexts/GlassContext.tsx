"use client";

/**
 * @deprecated This module is kept for backward compatibility.
 * All glass functionality is now provided by `FTProvider` in `src/components/FTProvider.tsx`.
 * Import from there instead.
 */

export type { GlassMode, GlassContextType, GlassProviderProps } from '../components/FTProvider';
export { GlassProvider, useGlass } from '../components/FTProvider';
